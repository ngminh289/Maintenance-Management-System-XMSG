/**
 * auth.service.js — Nghiệp vụ xác thực: bcrypt, JWT, cookie httpOnly, email (skeleton).
 * TODO: bảng token xác thực email / reset password khi làm chức năng thật.
 */
const STUB =
  'Endpoint auth skeleton — triển khai theo rule/function.rule (JWT + nodemailer + verify Gmail).';

export async function authStub(action) {
  return { ok: false, message: `${STUB} (${action})` };
}
